a library of some sort?
mostly a ui lib thing that is way too over-engineered and has way much for what it actually will get used for. basically overdone, but its fine.
id say its basically a "learning" project. its something different than usual minecraft modding (blocks, items, entities), which i appreciate working on.
